import type { AdEventName, BannerOptions, GameHubSDKOptions, InterstitialOptions, RewardedOptions, SDKEvent } from "./types";
export declare class GameHubSDKClient {
    private config;
    private initialized;
    private lastInterstitialAt;
    private readonly events;
    private readonly adManager;
    private readonly analytics;
    init(options: GameHubSDKOptions): Promise<void>;
    on(name: AdEventName, listener: (event: SDKEvent) => void): () => void;
    showBanner(options?: BannerOptions): Promise<boolean>;
    showInterstitial(options?: InterstitialOptions): Promise<boolean>;
    showRewardedAd(options?: RewardedOptions): Promise<boolean>;
    removeBanner(containerId?: string): void;
    destroy(): void;
    private safeRequest;
    private showOverlay;
    private renderCreative;
    private handleClick;
    private ensureInit;
    private emit;
}
export declare const GameHubSDK: GameHubSDKClient;
export default GameHubSDK;
