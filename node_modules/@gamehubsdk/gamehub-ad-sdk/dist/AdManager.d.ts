import type { AdCreative, AdRequest, GameHubSDKOptions } from "./types";
export declare class AdManager {
    private getConfig;
    constructor(getConfig: () => GameHubSDKOptions | null);
    requestAd(request: AdRequest): Promise<AdCreative | null>;
    ping(url?: string): Promise<void>;
    private demoCreative;
}
