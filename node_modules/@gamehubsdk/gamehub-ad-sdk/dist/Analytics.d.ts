import type { SDKEvent, GameHubSDKOptions } from "./types";
export declare class Analytics {
    private getConfig;
    constructor(getConfig: () => GameHubSDKOptions | null);
    track(event: SDKEvent): Promise<void>;
}
